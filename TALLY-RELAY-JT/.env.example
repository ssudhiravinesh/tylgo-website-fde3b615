# Supabase credentials (service role key — NOT the anon key)
SUPABASE_URL=https://onucizagpgwdpcakskat.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Tally connection (VCC Client tunnel)
TALLY_HOST=localhost
TALLY_PORT=9000

# Polling interval in milliseconds
POLL_INTERVAL_MS=5000

# Sales ledger name in Tally (must match exactly)
SALES_LEDGER_NAME=Sales Account
