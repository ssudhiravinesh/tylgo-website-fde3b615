# Supabase credentials (get from your Supabase project settings → API)
SUPABASE_URL=https://onucizagpgwdpcakskat.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Tally connection (VCC Client creates this tunnel)
TALLY_HOST=localhost
TALLY_PORT=9000

# Polling interval in milliseconds
POLL_INTERVAL_MS=5000

# The name of the Sales Account ledger in Tally (must match exactly)
SALES_LEDGER_NAME=Sales Account
